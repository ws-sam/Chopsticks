{
  "name": "chopsticks-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "preview": "next build && npx wrangler pages dev out --port 3000",
    "deploy": "next build && npx wrangler pages deploy out --project-name chopsticks-wokspec --branch main",
    "deploy:ci": "next build && CLOUDFLARE_API_TOKEN=$CF_API_TOKEN npx wrangler pages deploy out --project-name chopsticks-wokspec --branch main --commit-dirty=true"
  },
  "dependencies": {
    "next": "^16.1.6",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  }
}
